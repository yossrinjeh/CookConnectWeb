<form method="post" action="{{ path('app_information_personnele_delete', {'id': information_personnele.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ information_personnele.id) }}">
    <button class="btn">Delete</button>
</form>
