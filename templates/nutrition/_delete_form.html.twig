<form method="post" action="{{ path('app_nutrition_delete', {'id': nutrition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nutrition.id) }}">
    <button class="btn">Delete</button>
</form>
