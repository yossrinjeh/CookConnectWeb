<form method="post" action="{{ path('app_repas_delete', {'id': repas.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repas.id) }}">
    <button class="btn">Delete</button>
</form>
