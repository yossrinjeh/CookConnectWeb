<form method="post" action="{{ path('app_ajoutingredientrequest_delete', {'id': ajoutingredientrequest.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ajoutingredientrequest.id) }}">
    <button class="btn">Delete</button>
</form>
