<!DOCTYPE html>

<title>Edit Repas</title>

{% block body %}
    <h1>Edit Repas</h1>

    {{ include('rep/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_rep_index') }}">back to list</a>

    {{ include('rep/_delete_form.html.twig') }}
{% endblock %}
