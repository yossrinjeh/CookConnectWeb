<form method="post" action="{{ path('app_rep_delete', {'id': repa.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repa.id) }}">
    <button class="btn">Delete</button>
</form>
